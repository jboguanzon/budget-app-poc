from django.apps import AppConfig


class CategoriesConfig(AppConfig):
    """Configuration class for the `categories` application."""

    default_auto_field = "django.db.models.BigAutoField"
    name = "apps.categories"
