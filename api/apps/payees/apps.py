from django.apps import AppConfig


class PayeesConfig(AppConfig):
    """Configuration class for the `payees` application."""

    default_auto_field = "django.db.models.BigAutoField"
    name = "apps.payees"
